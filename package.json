{
  "name": "@fomoxa/net",
  "version": "0.1.0",
  "description": "The Fomoxa runtime for Node: framing, schema handshake, heartbeat and a non-blocking TCP/UDP tick loop. Codecs come from fomoxac.",
  "license": "Apache-2.0",
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=22"
  },
  "files": [
    "dist",
    "src",
    "README.md",
    "LICENSE"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "test": "node --test \"tests/*.test.ts\"",
    "build": "tsc",
    "check:examples": "tsc --ignoreConfig --noEmit --target ES2022 --module nodenext --moduleResolution nodenext --strict --types node --rewriteRelativeImportExtensions examples/schema.ts examples/echo-server.ts examples/echo-client.ts",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/fomoxa/typescript"
  },
  "homepage": "https://fomoxa.github.io",
  "keywords": [
    "fomoxa",
    "protocol",
    "binary",
    "networking",
    "udp",
    "tcp"
  ]
}
